// src/Matches.js

import React from 'react';
import './Matches.css';

const Matches = () => {
    return (
        <div className="matches">
            <h2>Matches</h2>
            <p>View matched listings here.</p>
            {/* Add components for viewing matches */}
        </div>
    );
};

export default Matches;
