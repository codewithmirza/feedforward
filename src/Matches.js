// src/Matches.js

import React from 'react';

const Matches = () => {
    return (
        <div>
            <h2>Matches Page</h2>
            {/* Add matches page content here */}
        </div>
    );
};

export default Matches;
