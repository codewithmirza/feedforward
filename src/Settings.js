// src/Settings.js

import React from 'react';
import './Settings.css';

const Settings = () => {
    return (
        <div className="settings">
            <h2>Settings</h2>
            <p>Manage your account and privacy settings here.</p>
            {/* Add form for managing settings */}
        </div>
    );
};

export default Settings;
