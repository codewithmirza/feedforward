// src/Settings.js

import React from 'react';

const Settings = () => {
    return (
        <div>
            <h2>Settings Page</h2>
            {/* Add settings page content here */}
        </div>
    );
};

export default Settings;
